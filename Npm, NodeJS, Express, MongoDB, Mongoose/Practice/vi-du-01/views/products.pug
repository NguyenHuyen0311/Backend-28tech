extends layouts/default.pug
include mixins/box-head.pug

block main 
    +box-head("Trang sản phẩm", "Đây là đoạn mô tả ngắn trang sản phẩm")

    ul 
        each item in products
            li #{item.title} - #{item.price}$